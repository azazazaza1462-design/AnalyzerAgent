namespace Lendlogic.Analyzers.DataAccess.Enums;

/// <summary>The reviewer's ground-truth outcome for a job — the training label.</summary>
public enum DecisionOutcome
{
    Approved,
    Rejected,
}
