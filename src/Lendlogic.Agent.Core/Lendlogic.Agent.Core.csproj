<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Lendlogic.Analyzers.DataAccess\Lendlogic.Analyzers.DataAccess.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Claude vision (official Anthropic C# SDK). PDFs are sent as native
         document content blocks, so no PDF->image rasterization is needed. -->
    <PackageReference Include="Anthropic" Version="12.*" />
  </ItemGroup>

</Project>
